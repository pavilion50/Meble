# Centralny adres witryny — używany w meta tagach (canonical, Open Graph,
# Twitter Card), danych structured data (JSON-LD) oraz w generowanych
# plikach robots.txt i sitemap.xml (patrz vite.config.js).
#
# Po zakupie docelowej domeny wystarczy zmienić poniższą wartość
# i wykonać ponowny build (`npm run build`) — nie trzeba ręcznie
# podmieniać adresu w żadnym innym pliku.
VITE_SITE_URL=https://kw-meble.bogdanfedik92.chatgpt.site
